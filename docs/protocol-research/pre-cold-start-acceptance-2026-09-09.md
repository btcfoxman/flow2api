# 3.5 pre 冷启动登录验收与上传超时修正

## 已验证的范围

2026-09-09，Flow2API `23e375d` 与 tupdater `080afba` 的 CI 和部署均成功。
3.5 实际运行前者的 SHA 镜像。三个项目保持分离，插件仅构建本地包。

目标账号 43 在专用 Profile 中完成正常 Google 网页登录，并通过 Labs
官方登录按钮更新该浏览器自身的 OAuth 会话。身份、新站项目 RPC 和
OAuth 余额接口均验证通过；余额 46。没有从源浏览器复制登录凭据。

随后执行真正的冷启动验收（不消耗积分）：

1. 正常停止目标浏览器和桌面 helper，确认两个进程退出。
2. Profile `exit_type=Normal`，根域 SID 类 Cookie 有 3 项，网页专用模式已启用。
3. 通过已部署 Native CDP 代码、原 Profile、原账号代理重新启动。
4. 项目 RPC 和 OAuth 余额查询均成功，余额仍为 46。
5. 再次正常停止，确认进程退出、`exit_type=Normal`。

这是修正浏览器账号协调行为后的真实登录冷启动验证，已不再只是同进程
成功。旧的同进程图片/视频验证记录不能替代最新生产请求验收；账号 43
在上述操作期间仍禁用于生产调度，排队任务尚未整体恢复。

## 另一个线上问题：参考图上传超时被终止

日志 26868 对应排队记录 721，2026-09-09 05:11:53 UTC：参考图上传
`attempt=1/5 cause=TimeoutError`，外部 502，队列被标记失败。
空消息的 Python/CDP `TimeoutError` 没有被按类型识别，因而跳过了重试。

- 仅在参考图上传处补充类型识别；不扩大付费生成提交的超时重试规则。
- 上传重试耗尽时，明确的上传超时映射 503，使异步队列保留原任务、
  原素材和排队次序后重试。参数错误仍为 400，其他上传错误仍为 502。
- 未自动重投结果不明或已经提交的生成任务。
- 记录 721 的原请求已被旧逻辑清空，不凭空重建或伪报成功。

补充测试覆盖空消息超时、重试上限、上传/生成超时区分和队列保留。
实际队列消化、新的生产图片/视频成功及其他账号的同步质量仍须持续验收。
