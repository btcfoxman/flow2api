# Native 独立登录及图片/视频验收（进行中）

> 本文记录上一轮准备阶段；本轮验证与重启回归的最新状态见 [pre-native-runtime-verification-2026-09-09.md](pre-native-runtime-verification-2026-09-09.md)。

用户要求保持三个项目分离，继续验证直到 flow2api 调用图片、视频成功。本记录不是成功验收报告。

## 当前状态

- 3.5 使用原候选镜像 `pre-fix-20260909`；没有把生成搬到源 g1 浏览器，也没有再次复制源 Google 会话。
- 临时隔离账号 43 的生产调度；原 `is_active/image_enabled/video_enabled` 备份保存在 `/app/tmp/relogin-canary-20260909/account-43-isolation.json`。原值均为 1，目前图片、视频开关为 0，以防同步接口自动启用造成并发抢用。
- 在 3.5 flow2api 容器上启动独立显示器 `:143`，浏览器直接打开真实持久目录 `/app/tmp/native_cdp_profiles/token-43`，使用账号配置代理。启动时未调用 Cookie seed，不导入源站 Cookie。
- 当前停在 Google 账号选择/登录页面，尚未建立可验证的目标会话。密码与二次验证由用户在登录桌面内完成，不通过聊天或日志收集。
- noVNC/x11vnc 仅监听服务器回环地址。通过已认证 SSH 映射到本机 `127.0.0.1:16043`；本机 `19243` 是同一浏览器的私有 CDP 转发，没有开放新公网端口。
- 登录入口为 `http://127.0.0.1:16043/vnc.html?autoconnect=1`，已在用户 g1 浏览器新标签页打开。外层浏览器只是桌面显示入口，实际登录和生成均在 3.5 浏览器内。
- 安装了当前容器的临时桌面依赖（x11vnc/noVNC/websockify）；不属于正式镜像修改，容器替换后需重建诊断桌面。未改工作流、主服务监听配置或其他项目。

## 本轮代码修复（本地，尚未部署）

回归结果：325 项通过（21.01 秒），`git diff --check` 无错误。当前等待目标登录，尚未提交本轮付费生成测试，图片/视频验收未完成。

1. 新增 `native_session_state` 标记校验，绑定账号与代理配置摘要，不包含 Cookie、Token 或设备密钥。
2. 显式采用本地会话的 Profile 不导入外部 ST/Google Cookie；例行或风控重启保留登录目录。代理配置不符则停止，不删除本地登录态。标记损坏也不能静默退回 Cookie 导入。
3. 插件同步接口针对显式本地会话账号返回 409，不能覆盖认证资料或自动启用该账号；其他账号继续使用原同步方式。
4. Native 的 Token 刷新、余额刷新、项目准备统一绑定账号实际浏览器代理；上下文结束后恢复原值，不串用其他账号代理。
5. Native ST 转 AT 不再因代理连接失败而回退直连。

标记文件 `.flow2api-local-session.json` 只可在核验目标账号身份、真实 RPC 与代理后由操作人员建立；本轮尚未建立或宣称登录通过。结构为 `version=1`、`token_id`、`proxy_sha256=SHA256(实际配置代理字符串)`。不得因为 Cookie 字段齐全或页面存在缓存 bootstrap 就启用。

## 后续执行次序

1. 用户在目标桌面完成账号 43 登录。运行 `output/playwright/verify_destination_login.py`，核验身份、同账号项目 RPC 和余额接口；脚本不打印凭证。
2. 如果 Flow 新站登录通过但旧 Labs OAuth 仍失效，先验证目标浏览器自身的 Labs 授权会话，不把 XSRF 当 OAuth、不复制源 Cookie、不靠伪造有效 AT 绕过检查。
3. 目标会话通过后才保护该 Profile、同步目标自身有效认证信息；停止登录辅助进程，确保浏览器正常退出落盘后部署新代码。全程保持账号隔离，防止新代码部署前外部旧快照覆盖新登录。
4. 先按原任务 ID 查询并恢复已出片任务，保留原水印/缓存后处理要求，不能重做生成。
5. 通过 flow2api 的真实请求链测试最低规格图片，再测试一条 360p/4 秒视频。检查成功终态、可用文件、真实余额变化和成功/失败计数；不将 HTTP 200 提交受理计为最终成功。
6. 最后恢复生产调度并清理测试进程/SSH 转发。不能删除持久 Profile；记录恢复结果和最终运行版本。

## 诊断进程

- 服务端辅助代码：`/app/tmp/native-login-43/login_worker.py`；控制状态：同目录 `state.json`，只含 PID/端口/目标信息。
- 服务端停止：通过同目录 `stop` 标记触发正常关闭，或向已校验的 helper PID 发送 SIGTERM；辅助程序负责关闭自己的浏览器、VNC 和 Xvfb，不操作其他账号进程。
- 本地转发：`output/playwright/native_login_tunnel.py`，SSH 凭证仅通过标准输入传递，限定目标 192.168.3.5 和固定回环服务。
- 不要在登录期间重新部署容器、删除 Profile、运行上一轮 Cookie 复制探针，或恢复账号生产调度。
