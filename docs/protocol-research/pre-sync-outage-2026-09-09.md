# 3.5 pre：插件同步与队列阻塞核查

## 现场证据（2026-09-09 06:00–06:22 UTC）

- 本地 g1-1hua / `D:\tmp\g-41` 实际加载扩展 1.2.1，未加载此前构建的 1.2.2。
  旧扩展缺少 `https://google.com/*` 权限，扩展读取不到根域 SID，
  但同一浏览器的只读 Cookie 检查能看到该会话。旧扩展日志连续 HTTP 400，
  不代表同步已写入。多个本地 Profile 共用这一未打包扩展目录。
- 已备份并原地更新实际加载目录为 1.2.3，Chrome 重新加载后确认版本、
  根域权限、根域 SID 可读，原账号代理端口 20001 不变；私有默认值未改。
  扩展只本地构建，不提交连接 Token 或其他凭据。
- 在账号各自代理上，只读调用 `/auth/session` 和余额接口：账号 1、5 的
  会话响应仍包含 AT，但到期时间已过去，余额接口实际返回 401；账号 7、8
  的同出口余额验证成功。新站网页登录不等于旧 Labs OAuth 已续期。
- 账号 7 原队列任务 `flow2api-submit-b4f376e8c61345618988805c5bb57171`、
  账号 8 原队列任务 `flow2api-submit-e16253a351e74b728930061f50d41e1d`
  已到 completed，而非仅提交成功。后续仍有等待任务持续进入，不能仅由
  队列长度判断是否消化。
- 新的最终失败含 `PUBLIC_ERROR_MINOR`；历史近期失败还含
  `PUBLIC_ERROR_AUDIO_FILTERED`。它们不能被当成账号失效或代理 429。
  [Google 的视频安全说明](https://cloud.google.com/vertex-ai/generative-ai/docs/video/responsible-ai-and-usage-guidelines)
  和 [Flow 使用说明](https://support.google.com/flow/answer/16353333?hl=en)
  说明了人物过滤及音频无法生成等限制。内部枚举分类以本次日志为依据；
  不据此认定用户素材违规，也不尝试绕过上游限制。

## 修复

1. 插件同步在 ST 换 AT 之前绑定提交的账号代理，覆盖余额验证和新账号创建；
   ContextVar 在退出/异常时恢复，并验证并发账号互不串代理。
2. Native 模式旧客户端省略代理时，仅当 ST 已匹配现有账号才沿用该账号的
   路由；新 ST 无法确认账号时要求提供 `captcha_proxy_url`，不走默认出口。
3. 拒绝已过期、到期时间无效、缺少 AT 或余额认证失败的会话；先验证，后
   写入及自动启用。网络验证失败保留旧账号数据；成功同步刷新余额及等级，
   返回 `oauth_verified`、`account_active`，保留关闭自动启用的管理员设置。
4. 调度器在占用浏览器或积分前跳过已知不完整的同步快照。任务继续保留在
   FIFO 队列等待新快照；不清空、不批量强制启用，也不影响独立本地 Profile。
5. 新增人物及音频过滤分类，返回可理解的终态原因，不记成账号故障、不
   自动重复生成。正常成功/失败统计保持分开。
6. 插件说明缺权限、Labs 授权过期、代理问题及独立会话保护的区别；错误
   不再 5 秒后消失，也不把服务端仍禁用的状态展示为完全成功。

账号 43 的服务器独立登录标记及 Cookie 隔离不变。其余已过期或未登录的
源账号仍需正常重新授权及同步；代码不能凭空恢复撤销的凭据。

## 验证边界

本地 Python 回归 358 项通过，扩展 Node 测试 16 项通过。线上生成的观察
来自原有队列，未追加付费测试视频、未改写已终止的内容拒绝任务。
本记录不等于全部账号、整体容量已恢复；部署后仍需检查镜像版本、公开任务
终态、队列进展及源凭据修复情况。原始凭据、Cookie、签名下载 URL 均不入库。
